spring:
  application:
    name: javaproject

  datasource:
    url: jdbc:postgresql://localhost:5432/books
    username: postgres
    password: kamysheva05
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080